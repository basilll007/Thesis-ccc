Step 1 correctness gate: at top_frac=1.0, the new filtered_score_for_labels function reproduces the existing edge_observed_score for all 22 interactions with a maximum absolute difference of 1.78e-15 (tolerance 1e-9) -- PASSED. At top_frac=0.5 (fixed, GNN-confidence-filtered edges only), filtered_z_score correlates with the original edge_z_score at Spearman rho=0.953 (p=1.21e-08) across the 16 CXCL12-CXCR4 rows. Among the 5 rows with the highest original edge_z_score, median |edge_z_score|=7.497 vs median |filtered_z_score|=6.049 -- filtering did not sharpen the already-established top signal. CD274->PDCD1 (SANITY CHECK -- NOT SCORED): filtered_observed_score stayed at essentially 0 for all 6 rows (confirmed, all < 1e-6) and remained non-significant (confirmed), as expected since this is a panel-detection limit (near-zero PDCD1 expression) rather than a spatial-contact limit that GNN-confidence filtering could ever change. Pre-registered success criterion (CXCL12-CXCR4 only): (a) rho>0.7 = True (actual 0.953); (b) filtering sharpens the top-5 signal = False. Overall verdict: PARTIAL.
